import { NavLink } from "react-router-dom";
import { useCarAction } from "../../provider/carProvider";

const Login = () => {

  return (
    <div>
      <p>login page</p>
    </div>
  );
};

export default Login;
